<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.hotelRestSystem"
        tools:targetApi="31" >



        <!-- first activity: splash screen -->
        <activity
            android:name=".activity.MainActivity0_animSplashScreen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================================ -->
        <!-- second activity: login -->
        <activity
            android:name=".activity.MainActivity1_login"
            android:exported="true" >
        </activity>

        <!-- ============================================================ -->
        <!-- third activity: signup -->
        <activity
            android:name=".activity.MainActivity2_signup"
            android:exported="true" >
        </activity>

        <!-- ============================================================ -->
        <!-- fifth activity: home -->
        <activity
            android:name=".activity.MainActivity3_home"
            android:exported="true" >
        </activity>

        <!-- ============================================================ -->
        <!-- fifth activity: home -->
        <activity
            android:name=".activity.MainActivity4_rent"
            android:exported="true" />

    </application>

</manifest>